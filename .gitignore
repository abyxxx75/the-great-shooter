# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp

# Local tooling — the dev server is a local convenience, not part of the site
.vercel/
node_modules/

# Source video is 30MB — too heavy for the repo and for visitors.
# The About section falls back to hero-poster.jpg until a compressed
# hero.mp4 is added, which is NOT ignored and should be committed.
assets/video/*.mov
